<Window x:Class="ShopApp_4ISIP322.SearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShopApp_4ISIP322"
        mc:Ignorable="d"
        Title="SearchWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Параметры поиска -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" >
            <TextBox x:Name="txtSearchName" Width="200" />
            <ComboBox x:Name="cmbSearchCategory" Width="200" DisplayMemberPath="CategoryName"/>
            <Button Content="Найти" Click="btnSearch_Click" Width="80"/>
            <Button Content="Сбросить" Click="btnReset_Click" Width="80"/>
        </StackPanel>

        <!-- Результаты поиска -->
        <DataGrid Grid.Row="2" x:Name="dgSearchResults" AutoGenerateColumns="False" Margin="0,10,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ProductID}" Width="50"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Категория" Binding="{Binding Category1.CategoryName}" Width="*"/>
                <DataGridTextColumn Header="Цена" Binding="{Binding Price}" Width="100"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="3" Content="Закрыть" Width="100" HorizontalAlignment="Right" 
                Margin="0,10,0,0" Click="btnClose_Click"/>
    </Grid>
</Window>
